[workspace]
members = ["src-tauri"]
resolver = "2"

[profile.dev]
incremental = true
opt-level = 0
debug = true

[profile.dev.package."*"]
opt-level = 1
debug = false

[profile.release]
opt-level = 3
lto = "thin"
panic = "abort"
codegen-units = 1
debug = "line-tables-only"
